/**
 * Re-export of the shared DiffComputer module.
 * The implementation lives in `src/shared/utils/diff/` so it can be consumed
 * by both the CLI (Ink) and the webview (React).
 */
export * from "@shared/utils/diff/DiffComputer"
